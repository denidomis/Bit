export default function Base2({
  atributas9,
  atributas1 = "Pridekite atributas1",
  atributas2,
  atributas3,
}) {
  return <h1>{atributas1}</h1>;
}

// Sukurti komponentą, kuris gauną vieną props. Props yra bet koks tekstas, kuris komponente atvaizduojamas h1 tage.
